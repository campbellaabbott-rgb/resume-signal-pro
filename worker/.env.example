SUPABASE_URL=https://bwhdazbotpblihdxcmho.supabase.co
# Service role: agent_claim_submission and agent_mark_uncertain are service-role
# only, and the worker writes submitted_at. Never ship this key to a browser.
SUPABASE_SERVICE_ROLE_KEY=
WORKER_ID=worker-1
# Pause between applications. Courtesy to the employer's server, and it keeps a
# candidate's batch from arriving as a burst.
APPLY_GAP_MS=20000
