{
  "name": "resumebooster-apply-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Drives real application forms for released packets. Separate service because Supabase edge functions are Deno with no browser, and every zero-CAPTCHA vendor builds its form in JavaScript.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "playwright": "^1.62.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
